{
  "name": "funquiz",
  "license": "UNLICENSED",
  "scripts": {
    "init": "graph init --from-contract <CONTRACT_ADDRESS>",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy funquiz --node https://api.subgraph.somnia.network/deploy --ipfs https://api.subgraph.somnia.network/ipfs --deploy-key <APIKEY_KEY>",
    "create-local": "graph create --node http://localhost:8020/ funquiz",
    "remove-local": "graph remove --node http://localhost:8020/ funquiz",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 funquiz",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
